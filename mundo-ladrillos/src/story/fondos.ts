// Fondos reales de la película embebidos en base64 (GENERADO por genfondos.mjs).
// En el repo va VACÍO (los fotogramas de la peli son material privado);
// se rellena solo para las entregas. En local se usa la ruta /fondos/… .
export const FONDOS: Record<string, string> = {};
