// Vídeo de intro = el arranque real de la peli (logo → título → estudio del
// Rabino). Es MATERIAL PRIVADO de la película: va VACÍO en el repo y se rellena
// (data URI) solo para las entregas. Si está vacío, el juego usa la intro 3D de
// estudio como respaldo.
export const INTRO_VIDEO = '';
