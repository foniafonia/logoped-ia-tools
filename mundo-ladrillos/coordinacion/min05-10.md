# Creador 5–10 · "El Jordán y los dos espías"

**Quién soy:** hilo del minuto 5–10 (los dos espías: Jordán → reclutamiento →
murallas de noche → cambio de traje → cruce por cuerda → treta del avión →
colarse → patrullas). Trabajo SOLO en `src/scenes/min05/` y en mi rama
`claude/min-05-10-jordan-spies-3kd08o`.

**En qué estoy (hito 2 · "con vida"):** dándole la vuelta grande tras feedback del
usuario (poca luz, sin sonido, jugabilidad pobre):
- **Luz nocturna** rehecha según `referencias/escenas/jerico-noche` y
  `jefe-guardia-01`: noche azul con luna+estrellas pero arquitectura CÁLIDAMENTE
  iluminada por faroles colgantes y braseros (legible, no negra).
- **Sonido**: motor de audio procedural propio (`audio/SoundEngine.ts`,
  WebAudio, sin assets) — ambiente noche/río, pasos, farol, alarma de guardia,
  avión, puerta, éxito/fallo. NO uso material privado de la peli. El LEAD puede
  cablear las voces reales por escena vía `StoryEngine`/`AudioManager`.
- **Jugabilidad**: colisiones con muros/props, medidor de detección en sigilo,
  mini-juego de equilibrio en la cuerda, interacción con tecla **E** + prompts,
  mandos táctiles (joystick + botón) para móvil, y **secuencia encadenada**
  (al lograr el objetivo, auto-avanza a la siguiente escena).
- **Personajes** según `referencias/personajes.md`: espías con **ropa de
  campamento** en 9–11 y **traje de sigilo** desde la 12; guardias con **casco
  cónico plateado + túnica a rayas rojo/amarillo + lanza**; Yehoshúa con barba
  blanca, turbante cobalto, capa y bastón.

**Qué ofrezco / hecho:**
- 8 escenas 3D de ladrillo jugables + preview autónomo (un solo HTML) + capturas.
- Mecánicas nuevas reutilizables: `StealthSystem`, `RopeCrossing`, `Distraction`.
- Todo aislado en `src/scenes/min05/`; NO toco archivos compartidos.

**Qué necesito de MUÑEQUERO (no urgente, tengo variantes locales de puente):**
- Skins canónicos de: **guardia de Jericó** (casco cónico + rayas rojo/amarillo),
  **jefe de guardia** (cresta de plumas + capa), y las variantes **campamento**
  de espía-1 (túnica beige/turbante gris-azul) y espía-2 (túnica marrón/turbante
  azul claro). De momento los compongo localmente con `MinifigureSkin`.

**Para el LEAD:** cada escena expone un `Min05Scene` (subtítulo, objetivo, spawn,
noche, cámara, jugador, `build(ctx)`); `isDone()`/`status()` resuelven los
objetivos nuevos (`distraer`, `sigilo`). Ver `src/scenes/min05/README.md`.
Mi rama incluye un merge de la rama del juego como base para compilar/capturar.

**Muñecos nuevos:** con OK del usuario ("como sea mejor"), he ADOPTADO en mi rama
el `characters/MinifigureFactory.ts` de `claude/munecos-ifepfa` (blob verbatim,
sin merge completo para no arrastrar conflictos de `main.ts`/`story`). Mis
escenas ya usan los skins canónicos (`YOSHUA_SKIN` con bastón/barba, `SPY_SKIN`/
`SPY2_SKIN`, `GUARD_SKIN`/`GUARD_CHIEF_SKIN` con casco cónico/plumas nativos) y
`Guard.ts` quedó reducido a crear el NPC con el skin. **LEAD:** cuando integres
`munecos-ifepfa` en la rama del juego, mi `MinifigureFactory.ts` es idéntico al
suyo (no habrá conflicto); mis escenas quedan listas.

**AUDIO DE LA PELI integrado (rama `claude/assets-min5-10` del lead):**
- `narracion_min5-10` (voces + música, 5:00) = fondo del tramo. Mi `SoundEngine`
  lo decodifica y lo reproduce POR SEGMENTOS: al entrar en cada escena salta al
  segundo del beat (`BEAT_LOCAL` en registry.ts: 9→7, 10→41, 11→140, 12→150,
  13→201, 14→212, 15→234, 16→245), para que la voz/música case con lo que se ve.
- Quité la música sintética (sonaba mal, feedback del usuario) y el `AudioManager`
  duplicado; el `SoundEngine` da ambiente + efectos + audio de la peli.
- Modelo de ritmo elegido por el usuario: "tú mandas, la peli acompaña" → cada
  escena espera a que el niño complete su mini-juego para avanzar (no se corta).
- PRIVACIDAD: `clips.ts` con el audio se usa SOLO al construir la entrega (swap
  temporal desde la rama de assets); NO se commitea. El repo queda con clips vacío.

## ✅ Respuestas al INTEGRADOR
- **¿Quién es el jugador en 9–11?** Ya viene en cada escena en `def.jugador`
  (léelo, no lo fuerces): **9 = `yoshua`** (contempla el Jordán), **10 = `yoshua`**
  (reparte la misión), **11 = `spy_camp`** (los espías se acercan a la muralla),
  **12–16 = `spy`** (sigilo). En la 12 hay cambio de traje en caliente
  (`ctx.setPlayerSkin('spy')`).
- **Skins:** ya uso los CANÓNICOS del muñequero, incluidas sus variantes
  `SPY_CAMP_SKIN`/`SPY2_CAMP_SKIN` (re-adopté su `MinifigureFactory` al día).
- **Audio de mi tramo (recordatorio):** `narracion_min5-10` (spine) + `BEAT_LOCAL`
  (registry) por escena. NO uses `bso_min5-10`/`voz`/`fondoClip` (los quité).
- **Cinemáticas** (avión, revelados): mis escenas llaman `ctx.cameraFocus?`/
  `ctx.cameraReveal?` (opcionales). Si las implementas en tu orquestador (como en
  mi `preview.ts`: estado `cine`, congelar input, enfocar objeto o `doReveal`),
  el niño ve el avión y los planos de establecimiento en el juego completo.

## 🎬 RECURSO reutilizable: "a veces el audio manda" (cámara cinemática)
Idea de dirección del usuario: el juego es jugable por defecto (tú mandas), pero
en MOMENTOS CLAVE la cámara toma el control unos segundos (sincronizada con la
peli) y luego lo DEVUELVE. Ya implementado y opcional en `SceneContext`:
- `cameraFocus(objeto, seg)`: sigue a un objeto en movimiento (p. ej. el AVIÓN
  de la treta) — la escena 14 lo usa.
- `cameraReveal(from,to,lookFrom,lookTo,seg)` + campo `Min05Scene.intro`:
  travelling de revelado al empezar la escena (planos de establecimiento). Lo
  usan la escena 9 (orilla del Jordán) y la 11 (muralla de noche).
Ambos son OPCIONALES: si el orquestador no los implementa, no pasa nada.
**Integrador:** este recurso encaja de perlas para tus grandes momentos —
el **derrumbe de la muralla**, el **cruce del Jordán (aguas partidas)**, el
**cordón rojo**… Reutiliza el mismo patrón (mira el preview: `doReveal`/cine).

## ⚠️ HANDOFF para el INTEGRADOR (tramo 5–10) — LEE ANTES DE RE-FUSIONAR
Sé que vas a tu bola: aquí tienes todo para meterlo bien sin preguntarme.

**Rama / hito:** `claude/min-05-10-jordan-spies-3kd08o`. Hito: 8 escenas + audio
REAL de la peli integrado y sonando (verificado headless, 0 errores).

**¿Toqué compartidos?** Solo `src/characters/MinifigureFactory.ts`, y es el del
MUÑEQUERO **adoptado verbatim** (idéntico) → al fusionar `munecos-ifepfa` no hay
conflicto real. **NO toqué `main.ts`** ni `StoryEngine.ts`. Todo lo demás en
`src/scenes/min05/`.

**AUDIO (cambió — re-sincroniza tu orquestador):**
- Clip único del tramo = **`narracion_min5-10`** (spine: voces+música). **NO
  existe `bso_min5-10`** ni voces por escena (no se cortaron; es intencionado).
- Para casar audio↔escena, al montar cada escena **reproduce el clip DESDE el
  segundo** `BEAT_LOCAL[numero]` (export en `registry.ts`):
  `9→7 10→41 11→140 12→150 13→201 14→212 15→234 16→245`.
- **`SceneContext` ya NO lleva `film`**, y las escenas **ya no usan `voz`/
  `fondoClip`** (los borré). El audio lo lleva `ctx.sound` (mi `SoundEngine`).
- **Reutilizable:** mi `SoundEngine` ya hace el salto por offset —
  `sound.loadFilm('narracion_min5-10')` (una vez) y en cada escena
  `sound.playFilmFrom(BEAT_LOCAL[numero])`. Puedes usarlo tal cual o replicar la
  idea con tu `AudioManager` (necesitarías reproducir desde offset).
- Gag del avión: clip corto **`m0510_14_avion`** (ya en `assets-min5-10`) →
  `sound.playClip('m0510_14_avion')` (baja el spine y suena encima).

En una frase: **usa `narracion_min5-10` + `BEAT_LOCAL`; no cablees
`bso_min5-10`/`voz`/`fondoClip` (quedaría mudo).**

## Mensaje para el LEAD (voces del tramo)
Con el SPINE COMPLETO (`narracion_min5-10`) + los tiempos ME BASTA. NO cortes las
8 voces granulares: el audio trae voz y música MEZCLADAS y trocearlo mete cortes.
Yo salto al segundo de cada escena (BEAT_LOCAL, seg locales = global−300):
9→7 · 10→41 · 11→140 · 12→150 · 13→201 · 14→212 · 15→234 · 16→245.
ÚNICO extra útil (y solo si es trivial): un clip CORTO **`m0510_14_avion`** con el
grito "¡UN AVIÓN!" para dispararlo cuando el niño hace la treta (bajo el spine un
segundo y suena encima). Opcional.

Ver bitácora completa del tramo: `coordinacion/min05-10-bitacora.md`.

**Preguntas:** ninguna bloqueante. (Confirmado con el usuario: el look "ninja" de
los espías es FIEL a la peli; se mantiene, solo en modo infiltrado desde la esc. 12.)

## NUEVO — cámara cinemática PORTÁTIL (para el orquestador)
Extraje el "a veces el audio manda sobre el juego" a un helper reutilizable:
`src/scenes/min05/props/CinematicCamera.ts`. Es autónomo (solo Three.js) y sirve
igual para mi preview que para tu ORQUESTADOR del juego completo — así el gag del
avión y los planos de revelado NO se pierden al integrar.

API mínima (engánchalo a tu bucle):
```ts
const cineCam = new CinematicCamera(camera);
ctx.cameraFocus  = (t, s)        => cineCam.focus(t, s);          // seguir objeto (avión)
ctx.cameraReveal = (f,t,lf,lt,s) => cineCam.reveal(f, t, lf, lt, s); // travelling revelado
// cada frame:
if (!cineCam.active) miControlador.update(dt);   // congela input mientras hay cine
escena.update(dt);                                // la escena SIEMPRE avanza
if (!cineCam.update(dt, jugador.x, jugador.z)) miCamaraNormal.update(jugador);
```
- Escenas con revelado de entrada (`Min05Scene.intro`): 9 (río→Jericó→Yehoshúa) y
  11 (murallas). Dispáralo con `cameraReveal(intro.from, intro.to, intro.lookFrom,
  intro.lookTo, intro.seconds)` al montar la escena.
- Gag del avión (esc. 14): al pulsar la treta hago `cameraFocus(avión, 2.4)` + subo
  el clip `m0510_14_avion`; la cámara enseña el avión sola y luego vuelve a manual.

## Detalle (subiendo el listón como los demás)
- Alfombras kilim (técnica del LEAD, `min00/textiles`) en tiendas 10 y 12.
- Puestos de zoco nocturno en la esc. 16 (`buildMarketStall` en `props/BrickProps`):
  hacen de cobertura para el sigilo y dan vida de calle. Con colisión.

## NUEVO — KIT DE HORIZONTE reutilizable (respuesta a la auditoría del muñequero)
Auditoría del muñequero, punto #1 ("mundos vacíos/lavados"): apliqué el kit.
`src/scenes/min05/props/Horizon.ts` → `buildHorizon(plastic)` devuelve:
- `group`: cinturón de DUNAS + CERROS/MESETAS de arenisca que rodean SIEMPRE al
  jugador (deterministas, sin Math.random; se iluminan solos con la luz día/noche
  de la escena; funden en la niebla). Nunca un muñeco en un descampado pálido.
- `daySky`: textura de cielo cálido de desierto (para `scene.background` de día).
- `horizonColor`: color para casar la niebla de día.
Ya cableado en mi preview (`applyLighting`): de día el fondo pasó de un liso
`0xf0d9a8` a cielo con color + dunas; de noche sigue `buildNightSky` + las dunas.
PORTÁTIL: el orquestador puede añadir `horizon.group` una vez y setear
background/fog por escena. NO incluye Jericó (las escenas 9/13 lo ponen de cerca).

Pendiente (siguiente hito): el "toque estrella" del escondite tras la ALFOMBRA
(spec del muñequero). Lo adaptaré a mi cámara 3ª persona (yaw≈π) para que el
bulto que respira se lea bien; decidiré escena (11 murallas / 16 calles).

## NUEVO — TOQUE ESTRELLA: escondite tras la ALFOMBRA (esc. 16)
Monté la spec del muñequero (`rug-hide.ts`) como escondite especial del sigilo,
adaptada a la cámara 3ª persona (yaw≈π): `src/scenes/min05/props/RugHide.ts`.
- Alfombra de kilim colgada (reutiliza `min00/textiles`), es un `HidingSpot` más.
- Al agacharte detrás, la tela se ABOMBA hacia la cámara (bulto que respira),
  con PIERNAS asomando y una manita. El cuerpo del jugador se oculta con el
  hook nuevo `ctx.setPlayerVisible(false)` (opcional, en types.ts).
- La 1ª vez que te escondes, la cámara hace un PLANO frontal automático del
  bulto y vuelve al control manual (usa `ctx.cameraReveal`, el recurso "a veces
  el audio/momento manda"). Reutiliza la CinematicCamera portátil.
Integrador: para que el efecto luzca necesitas implementar `setPlayerVisible`
(ocultar la minifigura) y `cameraReveal`; ambos OPCIONALES (si no, sigue jugable
sin el plano y con el jugador visible). RugHide es portable (Three.js + kilim).

## RESPUESTAS AL INTEGRADOR (9a/10a iteración)
Gracias por reintegrar la esc16. Dos cosas:

1. **¿Quién juega en 9–11?** Ya está decidido en los descriptores (`jugador`):
   - **9–10 = Yehoshúa** (contempla el Jordán y RECLUTA a los dos espías).
   - **11 en adelante = el espía** (`spy_camp` en 11–12, y `spy` de sigilo tras
     ponerse el traje en la 12). El relevo Yehoshúa→espías es a propósito.
   Si tu runner fuerza un jugador por tramo, respeta `def.jugador` por escena.

2. **Hooks nuevos que usa la esc16 (ambos OPCIONALES):**
   - `ctx.setPlayerVisible(v)` — ocultar la minifigura tras la alfombra.
   - `ctx.cameraReveal(...)` — el plano automático del bulto (ya lo tienes cableado
     para el avión/intro; la alfombra lo reutiliza).
   Si no los implementas, la esc16 sigue jugable (jugador visible, sin plano). Vi
   que tú ya montaste tu propio bulto de alfombra en la casa de Rahab: perfecto,
   son sitios distintos (tu casa de Rahab vs mi calle/zoco), no chocan.

Sin bloqueos por mi parte. Sigo subiendo "momentos con alma" en mi tramo (río vivo
con peces en el cruce esc13, cordón rojo de Rahab, verbos en 9 y 11).

## Verbos en 9 y 11 (cierre auditoría: "un verbo por escena")
Las escenas 9 y 11 dejan de ser solo "anda a la marca": al llegar, PULSA E para
un verbo de observación (reutiliza `ctx.cameraReveal`, vuelve a manual):
- Esc9: Yehoshúa OTEA Jericó al otro lado (la cámara cruza el río hasta la fortaleza).
- Esc11: los espías ESTUDIAN la muralla (recorre la coronación y las patrullas;
  estado "2 guardias y la puerta cerrada").
`isDone` de ambas ahora exige el verbo (no basta con llegar). Integrador: si tu
runner completa por proximidad, respeta el `isDone()` de la escena (ya lo hace vía
el hook `isDone` del StoryEngine).

## FIX audio (bleed del avión) + adopto la CHAPITA de parte
- **Bug:** `playFilmFrom` iba con `loop=true` y sin fin → la narración seguía de
  corrido y, si el jugador se demoraba, sonaba el AVIÓN (esc14) en escenas previas.
- **Fix:** ahora reproduce SOLO el segmento `[BEAT_LOCAL[n], BEAT_LOCAL[n+1])` y se
  funde a silencio al acabar (queda el ambiente). Nada de bleed entre escenas.
  Firma nueva: `playFilmFrom(off, endSec?, gain?, label?)`. Integrador: si usas mi
  `SoundEngine`, pásale el fin de segmento (beat siguiente) al montar cada escena.
- **Chapita de parte:** adopté tu `src/ui/SceneTag.ts` (verbatim) en mi preview.
  Añadí `SoundEngine.nowPlaying()` para el `getAudio` (muestra "escN @ Ns" o
  "🔇 solo ambiente"/"clip …"). El botón "📋 Copiar" da un reporte con escena +
  archivo + audio + posición para iterar en el sitio.

## ⚠️ LEAD — el clip `narracion_min5-10` NO CASA con el desglose (bloqueante audio)
Verificado con el usuario oyéndolo en el navegador (con la chapita de parte):
- La chapita marca **esc9 @ 21s** (escena 9, arranca en el beat 7, playhead 21s).
- A ese segundo se oye **"...es una noche tranquila... ¡un aviооón!"** = el GAG DEL
  AVIÓN, que tu desglose sitúa en la **escena 14 (local 212s)**.
- Medido por mí: el clip dura **300.0s** (MPEG1, 96 kbps). Mis offsets salen 1:1 de
  `referencias/desglose-escenas-peli.json` (9→7,10→41,11→140,12→150,13→201,14→212,
  15→234,16→245). O sea: **mis tiempos = tu tabla, pero el CONTENIDO del clip está
  desplazado** (el avión suena en ~seg 21, no en 212).

No puedo re-mapear a ciegas: el proxy me bloquea el STT y el chromium headless no
trae códec MP3, así que no puedo oír/alinear el clip yo.

**Necesito una de dos:**
1) Un `narracion_min5-10` **re-cortado** que empiece de verdad en el minuto 5:00 y
   case con el desglose (offsets 7..245), **o**
2) Los **segundos reales** de cada escena EN ESE clip (dónde empieza cada beat 9–16).

Mientras tanto he puesto **`USE_FILM_SPINE = false`** en mi preview (narración OFF)
para no soltar audio que no toca. Se mantienen ambiente + efectos + gemas + el gag
interactivo del avión (`m0510_14_avion`, clip corto de 4s, ese sí correcto).

## AUDIO — el clip está CONDENSADO (no es 1:1 con el desglose)
Calibrado de oído con el usuario: el clip `narracion_min5-10` EMPIEZA con el RÍO
(orilla, escena 9) y a los ~20s ya suena el gag del avión. O sea NO es minuto-a-
minuto del desglose; está condensado. Por eso ya no uso los tiempos del desglose:
uso VENTANAS REALES del clip por escena (`CLIP_SEG` en preview), calibradas de oído.
- esc9 = clip [0,15]s (río, cortado antes del avión). ✅
- resto: pendiente de calibrar con el usuario (pegatina) — mientras, esas escenas
  van con ambiente (sin voz de la peli) para no soltar audio que no toca.
LEAD: si el clip debía ser 1:1 con el desglose, está mal cortado; un clip alineado
lo simplificaría. Si no, con estas ventanas de oído vamos tirando.

## AUDIO — EVIDENCIA DURA (decodifiqué el clip yo mismo)
Instalé un decodificador MP3 (wasm) y analicé `narracion_min5-10`:
- Duración real: 300.0s. RÍO (calma) en [0,~11.5]s.
- **Cross-correlación del gag `m0510_14_avion` contra la narración → el grito del
  avión está en el SEGUNDO 22.2s del clip** (no en 212s como dice el desglose).
- El audio es casi continuo (sin silencios que separen escenas), así que el clip
  está CONDENSADO: río (esc9) → a los 22s ya el avión (esc14). Las escenas 10–13
  no tienen un tramo identificable en el clip.
Conclusión: el clip NO es 1:1 con el desglose. Asigné solo lo verificado
(esc9=[0,11.5]s río). Para tener voz/música por escena en 10–13/15/16 hace falta un
clip bien cortado (o los tiempos reales). LEAD: con esto puedes recortarlo fino.

## Recogido del informe del MUÑEQUERO (25-jul)
- ✅ **Chapita (`SceneTag`)** + `SoundEngine.nowPlaying()` ya enchufados en mi preview.
- ✅ **`buildRiverbank`** (world/Riverbank.ts) adoptado en la esc9: ribera de juncos/
  espadañas a lo largo del agua (era su parche para la orilla pelada).
- 📌 **Audio (nota del usuario 25-jul):** el HTML del cliente "La Caída de Jericó ·
  TuIA.tv" es SOLO referencia; el audio bueno es el NUESTRO (clips.ts + SoundEngine).
  Confirmo: yo uso `narracion_min5-10` de NUESTRO clips.ts, no del HTML del cliente.
  Ese clip está condensado (río→avión en ~22s) y NO casa con el desglose; el diseño
  de `mapa-partes` pide clips POR ESCENA (bso_min5-10 + voz_10/12/14…), que aún no
  existen. Cuando el LEAD los corte, los cableo por escena (mi SoundEngine ya toca
  clips por nombre). Mientras: esc9=río del clip [0,11.5], resto ambiente.
- ⏳ Pendiente de adoptar: `buildTent` (interiores esc10/12), `buildTavern` (Rahab),
  `Crowd`/`Market`/`StreetProps` (poblar la calle esc16), `buildHorizon` canónico.

## Poblar la calle (StreetProps del muñequero) — esc15 y esc16
Adopté `world/StreetProps.ts`: guirnaldas de farolillos cruzando la calle, ropa
tendida y pozo. Puestas en la esc16 (2 guirnaldas + 2 tendederos + pozo, que hace
de escondite del sigilo) y la esc15 (2 guirnaldas + tendedero). Luces controladas
(lights:2 por guirnalda) por rendimiento móvil. La calle pasó de sosa a zoco vivo.

## AUDIO — PREPARADO para meter los recortes (falta solo cortar del vídeo)
El usuario me da el AUDIO COMPLETO + TRANSCRIPCIÓN CON TIEMPOS del vídeo entero.
Mapeado por CONTENIDO (mis 8 escenas están repartidas por el vídeo, NO en el min
5–10; por eso el clip condensado no cuadraba). Ventanas reales (segundos del VÍDEO):
- 9  voz_09  [0:46–1:03]   «…el poderoso río Jordán, la tierra prometida»
- 10 voz_10  [3:41–4:16]   «Necesito hombres… espiar Jericó»
- 11 voz_11  [4:24–4:34]   «Es peligroso: si os descubren, os matarán»
- 12 voz_12  [4:44–5:00]   «¿Estás listo? — Vamos a cambiarnos»
- 13 (música/ambiente, sin diálogo)
- 14 voz_14  [5:12–5:37]   «Es una noche tranquila… ¡UN AVIÓN!»  (+ gag corto)
- 15 voz_15  [5:37–5:54]   «Vamos, ya es hora»
- 16 voz_16  [5:54–6:44]   «Miren estas huellas… ¿vieron algo raro?»
CABLEADO YA HECHO: `SoundEngine.playSceneClip(name)` + tabla `SCENE_CLIP` en preview.
Subtítulos reescritos a las frases reales. Al cortar los `voz_NN` y subirlos a
clips.ts (SOLO entrega, privado) suenan solos por escena. Sin ellos: ambiente, 0 err.

## 🤝 HANDSHAKE con SEGUNDO CEREBRO / Eli (niño sintético) — 26-jul
Hilo hermano en `claude/segundo-cerebro-playtester-71kljp` (lee/escribe SOLO por
tablón: `coordinacion/segundo-cerebro.md` + `coordinacion/eli-reportes.md`).
Mi tramo 5–10 YA EXPONE los hooks que necesita su harness. En el preview
(`src/scenes/min05/preview/preview.ts`) hay en `window`:
- `__loadNumero(n)` — carga la escena n (9..16) sin recargar página.
- `__probe()` → `{ numero, titulo, pos:{x,z}, goal:[x,z]|null, done, prompt,
  alarm(0..1), progress(0..1), gems:{got,total}, status, cine }`.
- `__walk(x,z,step)` — anda UN paso hacia (x,z) respetando colisión/límites
  (no teletransporta). Implementado con `PreviewController.stepToward`.
- `__interact()` — pulsa "E" (recoger gema / abrir / accionar).
- `__cine()`, `__filmReady()` — utilidades de cine/sonido.
Snippet mínimo para Eli (Playwright):
```js
await p.evaluate(n=>window.__loadNumero(n), 9);
const pr = await p.evaluate(()=>window.__probe());
if (pr.goal) await p.evaluate(([x,z])=>window.__walk(x,z,0.9), pr.goal);
if (/E\b|pulsa/i.test(pr.prompt||'')) await p.evaluate(()=>window.__interact());
```
Harness de referencia listo: `tools/playtester-min05.mjs` (juega las 8 escenas,
mide completado/tiempo/atasco/alarma, captura `pt-FAIL-N.png` de lo que no pasa).

## ✅ PLAYTEST del NIÑO SINTÉTICO (Eli) sobre el tramo 5–10 — 26-jul
Jugadas las 8 escenas con arnés headless (`tools/playtester-min05.mjs`, Node-driven).
**RESULTADO: 8/8 completables, 0 errores de consola.**
```
E9  ✅ Orilla del Jordán     [ir_a]     E13 ✅ Cruzar el río (cuerda) [cruzar]
E10 ✅ Reclutar a los espías [ir_a]     E14 ✅ La treta del avión     [distraer]
E11 ✅ Murallas de noche     [ir_a]     E15 ✅ Colarse por la puerta  [sigilo]
E12 ✅ Trajes de sigilo      [ir_a]     E16 ✅ Patrullas en las calles[sigilo]
```
### ⚠️ AVISO CRÍTICO a TODOS los hilos con arnés headless (Segundo Cerebro incluido)
Chromium **ESTRANGULA los timers de una página en segundo plano (~1 tick/seg)**,
aun con `--disable-background-timer-throttling`. Si tu bucle de juego usa
`setTimeout` **dentro** del navegador (`page.evaluate(async loop)`), solo da ~10
pasos en 18s y **TODO parece "atascado"** aunque el juego esté perfecto (falso
negativo). Yo caí en esa trampa: primeras pasadas daban 1/8, y era 100% el arnés.
**Solución fiable:** conduce los pasos **desde Node** (sus timers NO se estrangulan),
da varios `__walk` por tick y `page.bringToFront()`. Con eso: de 1/8 → 8/8.
El arnés `tools/playtester-min05.mjs` ya lo hace bien; reutilizadlo/copiad el patrón.

### Hooks (contrato acordado, ya en mi rama)
`__probe()` devuelve `pos`/`goal` como `[x,z]`, + `tipo`, `objetivo`(texto), `radio`,
`alarm`, `balance`, `progress`, `gems`, `status`, `prompt`. `__walk(x,z,step)->bool`
(colisión, no teletransporta), `__act()`/`__interact()` (E), `__jump()`.
**Novedad útil:** `__probe.goal` respeta `hud.goal` si la escena publica objetivo
DINÁMICO (siguiente sub-meta). Guía correctamente al marcador y al QA.

### Arreglos de esta pasada (a raíz del playtest)
- **esc12**: el spawn caía DENTRO del radio del perchero → se completaba en 0.1s sin
  andar. Perchero movido a (2,2): ahora hay recorrido dentro de la carpa.
- **esc10**: el marcador estático (0,4) quedaba a 4.47 de cada espía (fuera del radio
  3.5) → quedarse ahí no reclutaba a nadie. `hud.goal` dinámico → siguiente espía.
- **esc14**: el objetivo apuntaba a la puerta (0,22), saltándose la MARCA de la treta
  (0,-4) → nunca disparaba el «¡avión!». `hud.goal` en 2 fases: marca → puerta.

### Notas para Eli / MUÑEQUERO (matices, no bugs)
- El caminante en línea recta **no recoge gemas** (no desvía): normal; un niño real sí.
- **esc9** tarda ~14s: la meta (promontorio) está tras una roca; el jugador se acerca
  por el lado y llega al radio de «otear». Funciona; se puede pulir la aproximación.
- **esc15/16 (sigilo)**: superadas con alarma baja (≤0.34). El niño despistado se
  llenaría más la barra; los escondites (tinajas/pozo) están y funcionan.

## ◀ RESPUESTA A CEREBRO (26-jul) — sobre "Subir la CLARIDAD al nivel del 0–5"
Recibida la orden (P1 baliza, P2 aro de E, P3 más vida/luz, + vuestro QA).

**P1 + P2 — HECHO y pusheado** (commit de baliza). Añadí una **baliza de navegación
genérica** (`preview/NavBeacon.ts`), enchufada UNA vez al preview y alimentada cada
frame con el objetivo actual — no toco las 8 escenas:
- **Aro brillante + haz de luz + galón que bota SOBRE el objetivo** → "a dónde ir".
- **Flecha flotante sobre el jugador que apunta al objetivo** → "hacia allá".
- **En el sitio de pulsar E, todo se vuelve DORADO y late más rápido** → "pulsa aquí".
- Se oculta en cinemática y al completar.
- Aprovecha el **objetivo dinámico** (`hud.goal`): en escenas de varias sub-metas la
  baliza va guiando (esc10 → siguiente espía; esc14 → marca de la treta, luego puerta).
Captura de referencia: esc10 muestra aro dorado + galón + haz justo sobre el espía.

**QA de completado (vuestro dato pedido): 8/8, 0 errores de consola.** Vuestro bot
daba 1/8 y es LÍMITE DEL BOT: Chromium estrangula los timers de páginas en 2º plano
(~1 tick/seg) aun con `--disable-background-timer-throttling`; un bucle in-browser se
ahoga. **Solución: conducir los pasos DESDE NODE** (+ `page.bringToFront()`). Con eso
pasé de 1/8 → 8/8. Está en `tools/playtester-min05.mjs` (patrón reutilizable) y
detallado arriba en "AVISO CRÍTICO". Confirmado: el juego está bien, era el arnés.

**P3 (más vida/luz/color + camino claro en esc13) — EN CURSO**, siguiente entrega.
Nota: esc16 ya está viva (zoco, farolillos, ropa, pozo); el foco de P3 será los
**interiores marrones** (esc12 carpa) y **marcar el camino de la cuerda (esc13)**.

Dudas de visión: ninguna; si el galón/haz os parece intrusivo o preferís estilo del
0–5 (¿flecha 2D en pantalla en vez de 3D?), decídmelo por aquí y lo ajusto.

## ◀ RESPUESTA A CEREBRO (26-jul, 2ª) — P3 HECHO
**P3 (más vida/luz/color + camino claro esc13) — HECHO y pusheado.**
- **esc12 (carpa, era marrón/vacía):** guirnalda de farolillos cálidos, 2 luces
  cálidas, alfombra roja+dorada, cojines de color y baúl con tela. Base de luz
  interior subida un pelín. Ahora tiene calidez y color, no un marrón plano.
- **esc13 (cuerda):** el camino a cruzar se ve claro → haz cian al destino (baliza) +
  **guía del centro seguro más visible** (aro mayor + galón que bota, mismo lenguaje) +
  faroles a media travesía. El río ya estaba vivo (peces de color que saltan).
- **esc16** ya era la más viva (zoco/farolillos/ropa/pozo); sin cambios.
Resumen del tramo tras vuestra orden: **P1 baliza ✅ · P2 aro de E ✅ · P3 vida/luz ✅ ·
QA 8/8 ✅**. Quedo pendiente por el tablón (reviso cada pocos min). Si Eli juega otra
vuelta y algo no engancha, lo veo en `eli-reportes.md` y lo pulo.

## ▶ INPUT AL CEREBRO — feedback del playtest del HIJO (para que priorices y ordenes)
El usuario probó la build autónoma (HTML single-file, SIN audio de peli) con su hijo
en otro ordenador y va pasando experiencias. Las traslado con mi diagnóstico técnico
(NO he tocado código: espero tu orden priorizada).

1) **esc14 — "no se oyó 'mira el avión'".** Diagnóstico: son 2 sonidos: la VOZ "¡un
   avión!" es recorte de peli (`m0510_14_avion`) → NO va en la build sin audio; el
   RUIDO de avión es procedural (`SoundEngine.plane()`) y solo suena SI se dispara el
   gag (llegar a la marca + pulsar E). Opciones: (a) entregar build CON audio de peli;
   (b) reforzar gag procedural + gag VISUAL ("¡MIRA, UN AVIÓN!" en grande + guardias
   mirando arriba) para que funcione con o sin audio. Recomiendo (b) + (a) para entrega.

2) **esc9 y esc10 — "entorno desangelado, no hay conversación".** Faltan: (i) más vida/
   props/belleza (ahora se ven pobres), (ii) DIÁLOGO entre personajes (Yehoshúa y los
   espías). No hay sistema de conversación aún; propongo un hook `ctx.say(quién,texto)`
   con bocadillo, y guionizar la charla (esc10 = reclutamiento; esc9 = contemplar el
   Jordán). ¿Lo hago genérico para reusarlo en todo el tramo?

3) **esc11 — BUG "se sale por el borde derecho para llegar al sitio" (hace el juego
   fácil)** + "muy desangelado al final de la muralla". Diagnóstico: las bounds son
   x∈[-72,72] (muy abiertas) y la muralla-obstáculo solo cubre x∈[-40,40] → se rodea
   por la derecha, campo abierto y vacío. Fix propuesto: `bounds` por escena (acotar el
   corredor) + poblar el lateral derecho / final de muralla (torreón, tienda, props).

4) **esc12 — "la cabaña no se ve bien: más grande y más alta".** La carpa (`buildTent`)
   queda pequeña/apretada en cámara. Propongo escalarla (más alta y ancha) y/o alejar
   la cámara para que se vea entera.

**Pido:** prioriza (P1/P2/P3…) y ordéname en "▶ PARA MIN05". Yo ejecuto en cuanto lo
dejes escrito. Nota audio: sigue pendiente tu/decisión del usuario de meter la voz de
la peli (privada; solo build de entrega).

### (añadido) 5) esc15 — BUG GRAVE "se va por fuera de la escena y llega sin jugar"
El hijo reporta que en esc15 se puede salir del área y alcanzar el objetivo SIN hacer
el sigilo (truco/trampa demasiado fácil). Mismo patrón que esc11: bounds x∈[-72,72] muy
abiertas y los muros solo cubren x≈[-31,-9] y [9,31] (a z=8) → se rodea la puerta/los
guardias por el campo abierto. Fix propuesto: `bounds` por escena que fuercen el paso
por la puerta + cerrar los laterales con colisión/props. (Es la misma solución de
"bounds por escena" del punto 3; conviene aplicarla a TODAS las de sigilo/corredor:
esc11, esc15 y revisar esc16.)

## ◀ RESPUESTA A CEREBRO (26-jul, 3ª) — ejecutando la orden del tester real
Recibida y EN MARCHA. Estado por prioridad:
- **P0 — BUG borde/saltarse el sigilo (esc11/15/16): ✅ HECHO y pusheado** (commit
  `2c547e9`). Añadí `bounds` por escena (types + loadScene) y acoté el corredor:
  esc11 x[-32,26], esc15 x±24 (fuerza la puerta x[-5,5]), esc16 x±13 (calle central).
  **Verificado con arnés:** el jugador queda clamped al bound (no rodea por el campo) y
  las **8/8 siguen completándose, 0 errores**. El truco de "salir de la escena" cerrado.
- **P1 — esc14 gag VISUAL del avión: ✅ hecho** (cartel grande "✈ ¡MIRA, UN AVIÓN! ✈"
  billboard mientras dura la treta; los guardias ya miraban arriba y señalaban). Funciona
  con o sin voz. **esc12 cabaña más grande/alta: ✅ hecho** (toldo 1.35×1.55 + cámara a
  dist 26 para verla entera). Compila; pushear en breve tras verificar capturas.
- **P2 — esc9/esc10 más vida/props: EN COLA** (siguiente).
- **DIÁLOGO (esc9/10): a la espera de tu helper COMPARTIDO del LEAD** (no monto sistema
  propio, como ordenaste). Avísame cuando esté y lo engancho.
- **Audio voz peli:** entendido, solo entrega (privado); no lo uso en desarrollo.

Loop de coordinación: **reactivado** (chequeo del tablón cada 3 min). Sigo con P1→P2.

## ◀ RESPUESTA A CEREBRO (26-jul, 4ª) — ITERACIÓN DEL TESTER COMPLETADA
Toda tu orden priorizada, HECHA y pusheada:
- **P0 — bug borde/saltarse el sigilo (esc11/15/16): ✅** `bounds` por escena, jugador
  clamped, 8/8 completan, 0 errores. (commit 2c547e9)
- **P1 — esc14 gag VISUAL del avión: ✅** cartelón "✈ ¡MIRA, UN AVIÓN!" (flash de
  pantalla, siempre en cuadro) + billboard + guardias mirando arriba; con/sin voz.
  **esc12 cabaña más grande/alta: ✅** (toldo 1.35×1.55 + cámara dist 26). (commit 44ba23a)
- **P2 — esc9/esc10 más vida/props: ✅** esc10 campamento vivo (2 tiendas laterales,
  estandartes, puchero, barriles, +aldeanos); esc9 campamento a la izquierda + nenúfares/
  loto en el agua + palmeras + aldeanos. (commit 0ced164)
- **DIÁLOGO (esc9/10): ⏸️ a la espera de tu helper COMPARTIDO del LEAD.** En cuanto lo
  publiques, lo engancho (reclutamiento en esc10, contemplación del Jordán en esc9).
Sigo en el loop (chequeo cada 3 min). Cuando Eli rejuegue, recojo su feedback y pulo.

## ◀ RESPUESTA A CEREBRO (26-jul, 5ª) — PRECIOSO enchufado en el 5–10
✅ **`setupPreciousRender` enchufado** en el preview (commit f17ef97):
`const fx = setupPreciousRender(renderer, scene, camera, {ibl:false, bloom:{strength:.26, radius:.5, threshold:.9}})`,
`fx.render()` en el bucle, `fx.setSize()` en resize. `ibl:false` porque el IBL ya lo
gestiona mi preview por escena (envTex + applyLighting) — así no duplico PMREM ni piso
el env por escena.
**Móvil:** el helper va en modo LITE solo (IS_MOBILE → sin SMAA, bloom suave, IBL off
por Quality). No debería ahogar.
Antes/después: **noche (esc16) queda preciosa** (glow cálido en faroles/pozo, suelo con
reflejo, AA). Capturas enviadas al usuario.

### ⚠️ AVISO AL MUÑEQUERO (dueño del helper) — bloom por defecto LAVA el día
Con el bloom por defecto (strength .4 / threshold .82) mis **escenas DIURNAS brillantes
(arenal) se SOBREEXPONÍAN** (todo blanco/hazy). Lo arreglé bajando a **threshold .9 /
strength .26** (solo brillan luces/reflejos, no el arenal) y así el día queda nítido y
la noche mantiene el glow. Sugerencia para el helper compartido: **subir el threshold
por defecto** o exponer un preset "exterior día", que si no, cada tramo diurno se lava.
Si prefieres otros valores canónicos, dímelos y los uso.

- 🌙 **arranque noche 23:14 UTC** — hilo 5–10 VIVO. Ciclo 1: vestido del arenal exterior en esc9 (orilla) y esc10 (campamento) por REGLA Nº1; build OK, 8/8 sin errores de consola. Sigo en /loop 30m.

- 🌙 **07:01 UTC** — ciclo intenso 1: adoptada la BIBLIOTECA del muñequero (assets/tex, Clutter, Relic, Banner, tiling, PreciousRender canónico) y **bloom POR AMBIENTE** (día suave/noche con glow) usando sus presets. build OK · 8/8 · 0 errores. Sigo con el amueblado usando los props nuevos.

- 🌙 **07:43 UTC** — ciclo intenso 2: subtítulos de esc9/10/11/14 **alineados a la transcripción canónica** (referencias/transcripcion.md) con registro LATINO de la peli (era castellano "os"). build OK · 8/8 · 0 err. Refs privadas usadas SOLO en local (no al repo).

## 📌 NOTA AL MUÑEQUERO (dueño de MinifigureFactory) — skins vs biblia
Traje la biblia (referencias/peli.json). Mis muñecos usan tus skins canónicos (YOSHUA/SPY/…),
así que **no los sobrescribo** (evito incoherencia 0–5 vs 5–10; es tu ownership). Chequea si
casan con la biblia: Yehoshua piel #F1C40F, túnica azul real #2980B9, cinturón #8D4F2A, banda
azul; Espía Negro sigilo #1B2631 + detalles #D5F5E3; Espía Gris sigilo #566573/#2E4053, visor;
Jefe Guardia franjas rojo/amarillo #C0392B/#F1C40F + chaleco #34495E, casco cónico plateado con
pluma; Guardia común cota #7F8C8D + faldón #D35400, casco semiesférico. Si algo no casa, ajústalo
en tu factory y lo hereda todo el mundo.

## ❓ DUDA PARA EL CEREBRO (07:51 UTC) — solape reclutamiento 0–5 (adelanto) vs mi esc10
Contexto: decidiste que el **LEAD ponga el ADELANTO de los espías al FINAL del 0–5** con la
frase real de Yehoshúa ("necesito hombres discretos y valientes… espiar", 3:41). Pero **mi
esc10 "Reclutar a los espías" ES esa misma escena** (Yehoshúa arenga + los 2 espías aceptan),
con esa dialéctica. Si se juega 0–5 → 5–10 de seguido, el reclutamiento **sale dos veces**
(teaser al cerrar 0–5 y completo en mi esc10).
**Pregunta:** ¿cómo lo encajo? Opciones que veo:
  (a) 0–5 = solo TEASER corto ("próximamente… espías"), y mi esc10 = la escena COMPLETA de
      reclutamiento (recomendada: no duplica, el teaser engancha y mi escena desarrolla);
  (b) el reclutamiento vive en el 0–5 y mi esc10 pasa a ser otra cosa (p. ej. "los espías se
      preparan/parten"); 
  (c) dejarlo como está (asumir leve repetición).
Yo no lo decido (es encaje entre tramos). Mientras, **NO toco esc10** salvo pulido de vestido.
Sub-duda menor: confirmo que mi **esc9 (Orilla del Jordán) es el "arranque del río"** al que
enlaza el final del 0–5, ¿correcto? Si sí, cuido que la entrada de esc9 case con esa transición.

- 🌙 **07:51 UTC** — ciclo intenso 3: leído el tablón entero (sin orden nueva para 5–10); dejo DUDA
  de encaje 0–5↔esc10 para el cerebro. Referencias privadas en local (excluidas de git). Sigo /loop.

- 🌙 **07:56 UTC** — ciclo intenso 4: esc11 amueblada con Clutter del muñequero (hoguera de guardia + cajas/sacos/vasijas al pie de la muralla, antes desangelado). Arreglado el harness de captura (`?shot=1` quita el overlay). build OK · 8/8 · 0 errores.

- 🌙 **08:07 UTC** — ciclo 5: ENGANCHADO el diálogo compartido del LEAD (ui/Dialogue) y añadida la **conversación de reclutamiento en esc10** (Yehoshúa + espías, frases reales de la peli, auto-avance). Subtítulo se oculta cuando hay bocadillo. Resuelve "no hay conversación". build OK · 8/8 · 0 err.

- 🌙 **08:10 UTC** — ciclo 6: **conversación en esc9** (Yehoshúa contempla el Jordán + compañero, frases reales; cierra invitando a otear). Con esto **esc9 y esc10 ya tienen diálogo** (el "no hay conversación" del feedback, resuelto). build OK · 8/8 · 0 err.

- 🌙 **08:13 UTC** — ciclo 7: banter de apertura en **esc14** (los espías fingen calma → prepara el gag "¡UN AVIÓN!"), diálogo compartido. build OK · 8/8 · 0 err. Tramo 5–10 ya con conversación en esc9/10/14 + escenas vestidas + precioso + bordes/gag/carpa. **Idle: esperando novedades del cerebro** (no invento más relleno).

- 🌙 **08:17 UTC** — ciclo 8: **esc15 vestida** (el "desolado" del feedback): approach y calle con Clutter del muñequero (hoguera, cajas, sacos, vasijas), rocas/palmeras al borde y estandartes en la muralla; sin tapar la puerta ni los escondites. build OK · 8/8 · 0 err.

## ❓ DUDA PARA EL CEREBRO (08:18 UTC) — dificultad del SIGILO (esc15/esc16) "muere siempre"
El tester (hijo) dijo: **"en esc15 no se puede pasar el radar, es superfuerte y muere siempre"**
y **"esc16 es difícil llegar"**. Ojo: es **efecto secundario de mi fix P0** (cerré el borde →
ahora hay que pasar SÍ o SÍ por delante de los guardias, y los conos/alarma no estaban
calibrados para eso). Es **calibración de dificultad (borde de dirección)** y de noche no puedo
test-ear el "feel", así que **no la decido yo**.
**Propuesta lista para aplicar en cuanto des el OK** (conservadora, hace justo el sigilo sin
trivializarlo): en `mechanics/StealthSystem.ts` — (a) la **alarma sube más lento** y **decae
más rápido** cuando estás fuera de cono/escondido; (b) **conos algo más cortos/estrechos** en
esc15/16; (c) **más margen** en los escondites (radio). Y en esc16, un **objetivo más claro**
(abrir el refugio con E) como pediste. Dime el nivel de dificultad que quieres (fácil-niño / medio)
y lo cablveo + verifico con el arnés. Mientras, sigo con lo seguro (vestido/Regla Nº1).

## 🧩 CONTRATO DE INTEGRACIÓN (para el INTEGRADOR) — tramo 5–10
**Entrada del tramo:** `src/scenes/min05/registry.ts` exporta `MIN05_SCENES` (array EN ORDEN).
Cada escena cumple `Min05Scene` (ver `src/scenes/min05/types.ts`): `{ id, numero, titulo,
subtitulo, objetivo, spawn, build(ctx) }` → `build` devuelve `SceneInstance { group, update,
isDone, status?, hud?, dispose? }`. El StoryEngine avanza a la siguiente cuando `isDone()`.

**ORDEN de las 8 escenas (numero → archivo):**
9 orilla_jordan · 10 reclutar_espias · 11 murallas_noche · 12 trajes_sigilo ·
13 cruzar_rio · 14 treta_avion · 15 colarse_puerta · 16 guardias_calles.
- **PRIMERA escena = esc9** (spawn `{x:-8,z:-14}`, río Jordán) → **es el "arranque del río"**
  al que enlaza el final del 0–5 (campamento→caravana→**río**). Confirmado con el cerebro.
- **ÚLTIMA = esc16** (refugio de Rahab al fondo) → **enlaza con el 10–15** (Rahab).

**Dependencias EXTERNAS de las escenas** (deben existir en la base del LEAD; yo NO las modifico):
`characters/MinifigureFactory`, `materials/BrickPalette`, `materials/PlasticMaterialFactory`,
`world/Clutter`, `world/Riverbank`, `world/StreetProps`, `world/Tent`. Todo lo demás es local
en `src/scenes/min05/` (props/, mechanics/, skins.ts, audio/SoundEngine.ts).

**Hooks OPCIONALES de SceneContext** que usan mis escenas (degradan solo si no se proveen):
`say` (diálogo, `ui/Dialogue` del LEAD), `cameraReveal`/`cameraFocus` (cinemáticas),
`setPlayerVisible`, `setPlayerSkin`, `wantsInteract`, `addObstacle`. Además `objetivo.target`
+ `hud().goal` alimentan la BALIZA de guía (la monta el runtime; en mi preview es `NavBeacon`).

**NO integrar mi `preview/`** (es mi harness de dev/QA). La entrega son las escenas vía
`registry.ts`. Audio de peli (`audio/clips.ts`) va VACÍO en repo (privado; se rellena solo en entrega).

- 🌙 **08:20 UTC** — ciclo 9: dejado el CONTRATO DE INTEGRACIÓN (orden de escenas + entrada esc9=río + deps externas + hooks) para el INTEGRADOR, por la orden nueva del cerebro (08:15). Tramo autocontenido y pusheado.

- 📡 **08:31 UTC · PING al cerebro** — 5–10 vivo e **idle**. Tramo completo (8 escenas vestidas, precioso, conversaciones, subtítulos, bordes, gag, contrato de integración listo). **Pendiente tu respuesta:** ❓ dificultad del sigilo esc15/16 ("muere siempre") — tengo el fix conservador listo para aplicar en cuanto confirmes nivel (fácil-niño/medio). ¿Algo nuevo para mí?

- 📡 **08:33 UTC · heartbeat 5–10** — idle, tramo completo y pusheado. Sigo esperando tu ❓ dificultad del sigilo esc15/16 (fix listo). ¿Algo nuevo para mí?

- 🌙 **08:40 UTC** — ciclo 10 (cerebro ocupado ~10min, "seguid trabajando"): **conversación de los guardias en esc16** (descubren las huellas, gag cómico del finale, líneas reales 6:07), diálogo compartido. build OK · 8/8 · 0 err.

- 🌙 **08:43 UTC** — ciclo 11: **conversación en esc12** ("¿Estás listo? — Sí, vamos a cambiarnos", 4:44). Con esto el diálogo cubre **esc9/10/12/14/16**. build OK · 8/8 · 0 err. Tramo con conversación en todas las escenas con frase clave de la peli.

- 📡 **08:44 UTC · heartbeat 5–10** — idle, tramo completo (8 escenas vestidas, precioso, conversación en esc9/10/12/14/16, subtítulos, bordes, gag, contrato integración). Único pendiente: tu ❓ dificultad del sigilo esc15/16 (fix listo). ¿Algo nuevo?

- 🌙 **08:52 UTC** — ciclo 12: **SIGILO → FÁCIL-NIÑO** (orden del cerebro, respuesta a mi duda): alarma sube LENTO (0.28) y baja RÁPIDO al salir de vista; conos cortos/estrechos (esc15 14/17, esc16 12/16); al pillarte **NO mueres** → retroceso corto + aviso simpático "¡Casi! vuelve a intentarlo" (sin reset de escena). build OK · 8/8 · 0 err. **Cierra el "muere siempre" del tester.**

## ◀ RESPUESTA A CEREBRO (08:55 UTC) — sigilo FÁCIL-NIÑO ✅ HECHO y VERIFICADO
Aplicada tu orden (esc15/16 → fácil-niño): alarma sube lento (0.28) y baja rápido al salir de
vista; conos cortos/estrechos; al pillarte **NO mueres** → retroceso corto + aviso simpático,
sin fin de escena. **Verificado con el arnés Node-driven: E15 ✅ (alarma máx 0.08, era 0.95) y
E16 ✅ (alarma máx 0.07, era 0.34), 0 errores.** El "muere siempre" del tester, cerrado.
Con esto **el tramo 5–10 no tiene pendientes**: bordes, gag, carpa, vestido (Regla Nº1),
precioso, conversación (esc9/10/12/14/16), subtítulos fieles, sigilo fácil-niño, contrato de
integración. Quedo idle a la escucha; ping si hay algo nuevo.

- 🌙 **08:55 UTC** — ciclo 12b: playtester apuntado a file:// (robusto sin dev server) y usado para
  verificar el sigilo. E15/E16 ✅ con alarma ~0.07. Tramo 5–10 sin pendientes.

- 📡 **12:53 UTC · heartbeat 5–10** — reanudado. Probé suelo de arena texturizado (tex del muñequero) en esc10: build/0-err OK pero mejora AMBIGUA en cámara → revertido (no meto cambios estéticos dudosos solo). Confirmado: el diálogo del LEAD se ve perfecto en escena. Tramo completo, sin pendientes. Idle a la escucha. ¿Algo nuevo?

## ◀ RESPUESTA A CEREBRO (12:57 UTC) — registry.ts limpio para el integrador ✅
Atendida tu orden (▶ 5–10: "expón tu registry.ts limpio"): quité el código muerto `BEAT_LOCAL`
(reliquia del viejo clip condensado `narracion_min5-10`, que nadie lee) y actualicé la nota de
audio en `types.ts` al enfoque actual (clip por escena `voz_NN` + `SCENE_CLIP`; clips.ts VACÍO
en repo, privado). `registry.ts` queda mínimo: import de las 8 escenas → `MIN05_SCENES` en
orden + `sceneById`/`sceneByNumero`. build OK, 8/8, 0 errores. El integrador ya cosió mis E9–E16
sin fallos. Tramo redondo. Idle a la escucha; si el integrador pide algo, lo atiendo al momento.

- 🌙 **12:57 UTC** — ciclo 13: registry.ts limpio (fuera BEAT_LOCAL muerto) para el integrador; nota de audio al día. build OK · 8/8 · 0 err.

- 📡 **12:58 UTC · heartbeat 5–10** — idle. Integrador re-sincronizó mi min05 verbatim (sigilo fácil-niño ✅) sin fricción; registry.ts limpio. Tramo completo, redondo e integrado (E9–E16, 0 err). Sin petición pendiente. ¿Algo nuevo?

- 📡 **13:04 UTC · 5–10 CUMPLE el contrato del runner (13:01)** — mi `registry.ts` ya es la lista limpia y ordenada (`MIN05_SCENES`, spawn/objetivo por escena); NO necesito `runTramo()`. El integrador ya lo cose verbatim (Build OK, 12:59). 5–10 listo para el loader común, sin acción pendiente. Idle a la escucha.

- 📡 **13:08 UTC** — heartbeat: idle, 5–10 completo e integrado, sin novedad ni petición. A la escucha.

- 📡 **13:13 UTC** — heartbeat: idle, 5–10 estable e integrado, sin novedad. A la escucha.

- 📡 **13:18 UTC** — heartbeat: idle. Integrador adoptó el runner canónico y pasa MIN05_SCENES verbatim; smoke E9→E13 sin errores; su QA __dryRunAll no señala min05. 5–10 sólido e integrado. A la escucha.

- 📡 **13:23 UTC** — heartbeat: idle, 5–10 estable e integrado, sin novedad. A la escucha.

- 📡 **13:27 UTC** — heartbeat: idle, 5–10 estable e integrado, sin novedad. A la escucha.

- ✅ **13:33 UTC · QA regresión 5–10: 8/8 completan, 0 errores** (arnés Node-driven, tras conversaciones + sigilo fácil-niño + registry limpio). E9-E14 ✅; sigilo E15/E16 ✅ con alarma máx 0.07. Sin regresiones. Tramo sólido de punta a punta. Idle a la escucha.

- 📡 **13:36 UTC** — heartbeat: idle, 5–10 estable, integrado y QA-verificado (8/8). Sin novedad. A la escucha.

- 📡 **13:41 UTC** — heartbeat: idle, 5–10 estable e integrado, sin novedad. A la escucha.

- 📡 **13:46 UTC** — heartbeat: idle, 5–10 estable e integrado, sin novedad. A la escucha.
